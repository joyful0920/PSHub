- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

整数 n が与えられる。n の絶対値を出力する。

## 制約

- -100 ≦ n ≦ 100（整数）

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `-13` | `13` |
| `25` | `25` |

## 解法メモ

- 組み込みの `Math.abs(n)` で絶対値を求める。計算量 O(1)。
- （手動なら `n >= 0 ? n : -n`）
