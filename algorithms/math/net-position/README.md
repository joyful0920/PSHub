- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

座標 0 から始まり、ボタン A を押すと +1、ボタン B を押すと -1 移動する。
A を a 回、B を b 回押したあとの座標を出力する（= a - b）。

## 制約

- 0 ≦ a, b ≦ 100

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `10 3` | `7` |
| `2 6` | `-4` |

## 解法メモ

- 最終座標は `a - b`。計算量 O(1)。
