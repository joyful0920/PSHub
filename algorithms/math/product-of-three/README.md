- **カテゴリ**: 標準入力 / 算術

## 問題概要

1 行にスペース区切りで 3 つの整数 `a b c` が与えられる。3 つの積 `a * b * c` を出力する。

## 制約

- 0 ≦ a, b, c ≦ 100

## 入出力例

入力:
```
2 3 4
```
出力:
```
24
```

入力:
```
5 1 0
```
出力:
```
0
```

## 解法メモ

- 1 行を空白で分割し `map(Number)` で数値化、分割代入で受けて積を返す。計算量 O(1)。
