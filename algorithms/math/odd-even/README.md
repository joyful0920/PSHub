- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

正の整数 N が与えられる。N が奇数なら `odd`、偶数なら `even` を出力する。

## 制約

- 1 ≦ N ≦ 100

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `9` | `odd` |
| `4` | `even` |

## 解法メモ

- `n % 2 === 0` で偶奇を判定。計算量 O(1)。
