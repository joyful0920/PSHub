- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

スイッチは初期状態が `OFF`。1 回押すごとに ON / OFF が切り替わる。
n 回押したあとの状態を出力する。

## 制約

- 1 ≦ n ≦ 100（整数）

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `3` | `ON` |
| `8` | `OFF` |

## 解法メモ

- 奇数回なら `ON`、偶数回なら `OFF`。`n % 2` で判定。計算量 O(1)。
