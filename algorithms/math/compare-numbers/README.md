- **カテゴリ**: 標準入力 / 条件分岐

## 問題概要

正の整数 a, b が空白区切りで 1 行に与えられる。
大きい方の値を出力する。等しい場合は `eq` を出力する。

## 制約

- 1 ≦ a, b ≦ 1000（整数）

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `8 5` | `8` |
| `4 4` | `eq` |

## 解法メモ

- 空白で分割して数値化し、大小を比較。等しければ `eq`。計算量 O(1)。
