- **カテゴリ**: 標準入力 / 2 次元配列 / 算術

## 問題概要

2 行 2 列の行列が与えられる。

```
a b
c d
```

この 2 次行列の行列式 `a × d - b × c` を計算して出力する。

## 制約

- 各要素は整数、-1000 ≦ 各値 ≦ 1000

## 入出力例

入力:
```
3 2
1 4
```
出力:
```
10
```
（3×4 - 2×1 = 12 - 2 = 10）

入力:
```
5 5
2 2
```
出力:
```
0
```
（5×2 - 5×2 = 0）

## 解法メモ

- 各行を `split(" ").map(Number)` で数値化し、2 次元配列 `grid` にする。
- ネストした分割代入 `const [[a, b], [c, d]] = grid;` で 4 要素を取り出すと、
  式 `a * d - b * c` が問題文の定義とそのまま対応して読みやすい。計算量 O(1)。
