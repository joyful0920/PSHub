- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

整数 N と M が改行区切りで与えられる。N を M で割った余りを出力する
（割り切れる場合は 0）。

## 制約

- 1 ≦ N, M ≦ 100（整数）、M < N

## 入出力例

| N | M | 出力 |
| --- | --- | --- |
| `23` | `4` | `3` |
| `30` | `6` | `0` |

## 解法メモ

- 剰余（じょうよ）演算子 `%` で余りを求める → `N % M`。計算量 O(1)。
