- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

ある機器は 1 単位あたり毎時 25 のコストがかかる。
n 単位を 24 時間連続で使用したときの合計コストを求めて出力する。
（合計 = n × 25 × 24）

## 制約

- 1 ≦ n ≦ 10、n は整数

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `5` | `3000` |
| `2` | `1200` |

## 解法メモ

- 1 行目の n を読み、`n * 25 * 24` を出力する。計算量 O(1)。
