- **カテゴリ**: 標準入力 / 四則演算

## 問題概要

単価 a の商品を b 個買ったときの合計金額を出力する（合計 = a × b）。
入力は 1 行目に単価 a、2 行目に個数 b。

## 制約

- 0 ≦ a ≦ 500、0 ≦ b ≦ 500（ともに整数）

## 入出力例

| a | b | 出力 |
| --- | --- | --- |
| `120` | `3` | `360` |
| `0` | `99` | `0` |

## 解法メモ

- 2 行を数値化して掛けるだけ。計算量 O(1)。
