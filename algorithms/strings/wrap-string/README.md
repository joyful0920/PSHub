
- **カテゴリ**: 標準入力 / 文字列

## 問題概要

文字列 s と 1 文字 c が改行区切りで与えられる。
s の前後を c で 1 文字ずつ囲った文字列を出力する（`c + s + c`）。

## 制約

- 2 ≦ s の長さ ≦ 100、s は半角英字
- c は半角英字 1 文字

## 入出力例

| s | c | 出力 |
| --- | --- | --- |
| `cat` | `*` | `*cat*` |
| `dog` | `#` | `#dog#` |

## 解法メモ

- テンプレートリテラルで前後に c を連結するだけ。計算量 O(n)。
