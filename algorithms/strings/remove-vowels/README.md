- **カテゴリ**: 標準入力 / 文字列 / 正規表現

## 問題概要

文字列 S から母音（`a i u e o` の大文字・小文字）をすべて取り除き、
子音だけを連結した文字列を出力する。

## 制約

- 5 ≦ S の長さ ≦ 20、英字大小、子音を必ず含む

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `Beautiful` | `Btfl` |
| `Tokyo` | `Tky` |

## 解法メモ

- `replace(/[aeiouAEIOU]/g, "")`：母音の文字クラスを全箇所削除。計算量 O(n)。
