- **カテゴリ**: 標準入力 / 文字列

## 問題概要

整数 n と文字列 S が改行区切りで与えられる（1 行目 n、2 行目 S）。
S の先頭から n 文字を取り出して出力する。

## 制約

- 1 ≦ n ≦ 100、n ≦ S の長さ ≦ 100
- S は半角英字

## 入出力例

| n | S | 出力 |
| --- | --- | --- |
| `2` | `hello` | `he` |
| `5` | `keyboard` | `keybo` |

## 解法メモ

- `s.slice(0, n)` で先頭 n 文字を切り出す。計算量 O(n)。
