- **カテゴリ**: 文字列

## 問題概要

文字列 S が与えられる。1 文字ずつ改行して縦書きに出力する。

## 制約

- 1 ≦ S の長さ ≦ 20、S は半角アルファベット

## 入出力例

入力:
```
cat
```
出力:
```
c
a
t
```

入力:
```
go
```
出力:
```
g
o
```

## 解法メモ

- `split("")` で 1 文字ずつの配列にし、`join("\n")` で改行区切りに連結する。計算量 O(|S|)。
- `[...S].join("\n")` でも同じ。
