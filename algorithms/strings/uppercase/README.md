
- **カテゴリ**: 標準入力 / 文字列

## 問題概要

小文字の英字からなる文字列 s を 1 行受け取り、すべて大文字に変換して出力する。

## 制約

- 1 ≦ s の長さ ≦ 100
- s は半角英小文字のみ

## 入出力例

| 入力 | 出力 |
| --- | --- |
| `hello` | `HELLO` |
| `world` | `WORLD` |

## 解法メモ

- `toUpperCase()` は文字列全体に効くので、1 行そのまま変換すればよい。計算量 O(n)。
